import 'package:flutter/material.dart';

const kSecondContainer = BoxDecoration(
  borderRadius: BorderRadius.only(
    topLeft: Radius.circular(30.0),
    topRight: Radius.circular(30.0),
  ),
  color: Color(0xff262A33),
  // color: Colors.blue,
);

const kFirstContainer = BoxDecoration(
  color: Color(0xff212429),
);

const kTitleTextStyle = TextStyle(
  color: Colors.white,
  fontSize: 25.0,
  // fontWeight: FontWeight.w800,
);

// const kButtonTextStyle = ButtonStyle(textStyle: MaterialStateProperty(Color));